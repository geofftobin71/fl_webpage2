<section class="image-section{{ ' bg-alt' if (section.alternate_background_color) }}">
  <div class="wrapper text-wrapper">
    <figure>
      {% set image_path = section.image | safe %}
      <noscript><img src="{{ site.twic_url }}{{ image_path }}" alt="{{ section.alt_text }}" /></noscript>
      <img class="req-js" data-twic-src="image:{{ image_path }}" data-twic-step="50" data-twic-bot="contain=750x750" alt="{{ section.alt_text }}" src="{% lqip path=image_path %}" />
      {% if section.caption %}
        <figcaption class="caption" style="margin-top:0.3em">{{ section.caption }}</figcaption>
      {% endif %}
      {% if section.photographer %}
        {% set photographer = photographers.photographers | findPhotographer(section.photographer) %}
        {% if photographer and photographer.name %}
          <figcaption class="caption small">Photo Credit : 
          {% if photographer.webpage %}
            <a href="{{ photographer.webpage | safe }}" target="_blank" rel="noopener"> {{ photographer.name | safe }}</a>
          {% else %}
            {{ photographer.name | safe }}
          {% endif %}
          </figcaption>
        {% endif %}
      {% endif %}
    </figure>
  </div>
</section>
